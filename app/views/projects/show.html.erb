<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @project.name %>
</p>

<p>
  <b>Description:</b>
  <%= @project.description %>
</p>


<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>

<h2>ToDo items <%= link_to 'new one', new_project_todo_item_path(@project) %></h2>

<%= render 'todo_items/table', todo_items: @project.todo_items %>